
signal = ts_backfill(vec_avg(nws12_prez_4l), 504);
group = densify(bucket(rank(cap), range = "0.1667, 1, 0.1667"));
group_rank(signal, group) * (1 - group_rank(ts_delta(close, 3), group)) * group_rank(volume*close, group);
