
rt=power(fnd6_intc,3);

b=ts_mean(rt,22);

o=ts_std_dev(rt,12);

drt=-(ts_delta(b-rt,20)/24)+o * ts_delta(sqrt(rt),8)/8 * quantile(returns,driver=gaussian,sigma=1);

group=bucket(rank(cap),range="0.1667,1,0.1667");

group_backfill(group_rank(drt,subindustry),group,500)
